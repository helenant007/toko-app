import { css } from 'emotion';

export const styContainerGrid = css`
    display: grid;
    grid-template-columns: repeat(4, 180px);
    gap: 16px;
    width: 100%;
    height: 100%;
    min-height: 500px;
`;
